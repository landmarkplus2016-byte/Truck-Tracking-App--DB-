/**
 * ar.js — Arabic strings, to be filled in later (CLAUDE.md §9.1).
 *
 * Same keys as en.js. t() falls back to English for any key missing here, so this can be
 * translated a section at a time. Turning the chrome Arabic also means setting
 * <html lang="ar" dir="rtl">; the CSS already uses logical properties for that (§9.2).
 */

export default {};
